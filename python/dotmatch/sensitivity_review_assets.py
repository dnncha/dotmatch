"""Original, dependency-free assets for the portable sensitivity reviewer.

Keep CSS/JS as Python resources so wheels and source distributions include them
without a second bundler or package-data configuration. No scientific matcher
runs here; this interface only compares recorded outcomes and count tables.
"""
CSS = r'''
:root{color-scheme:light;--ink:#192d38;--muted:#576b75;--line:#d9e2e4;--paper:#fff;--wash:#f3f6f5;--accent:#165e60;--tint:#e6f1ef;--unique:#27777a;--ambiguous:#a16c19;--none:#7c8c98;--invalid:#846ca4;font:15px/1.55 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}body{margin:0;background:var(--wash);color:var(--ink)}button,input,select{font:inherit}button,select,input[type=file]{cursor:pointer}button{border:1px solid var(--line);background:#fff;color:var(--ink);border-radius:8px;padding:9px 13px;font-weight:600}button:hover:not(:disabled){background:var(--tint);border-color:#7aa6a1}button:disabled{cursor:default;opacity:.45}a{color:var(--accent);text-underline-offset:3px}input,select{min-height:43px;border:1px solid #a6b8bb;border-radius:8px;padding:9px 11px;background:#fff;color:var(--ink);max-width:100%}input:focus-visible,select:focus-visible,button:focus-visible,a:focus-visible,summary:focus-visible,[tabindex]:focus-visible{outline:3px solid #1a8589;outline-offset:3px}h1,h2,h3,p{margin:0}h1{font-size:clamp(1.9rem,3.1vw,3.05rem);line-height:1.13;letter-spacing:-.045em;max-width:940px}h2{font-size:1.3rem;letter-spacing:-.02em}h3{font-size:1.03rem}code{font: .86em/1.6 ui-monospace,SFMono-Regular,Consolas,monospace;overflow-wrap:anywhere}small,.muted{color:var(--muted)}[hidden]{display:none!important}.skip{position:fixed;top:-100px;left:20px;z-index:8;background:white;padding:12px}.skip:focus{top:12px}.app{max-width:1440px;margin:auto;display:grid;grid-template-columns:205px minmax(0,1fr);min-height:100vh}.rail{position:sticky;top:0;height:100vh;padding:32px 23px;border-right:1px solid var(--line);background:#f9fbfa;display:flex;flex-direction:column;gap:30px}.brand{display:flex;align-items:center;gap:10px;font-size:1.3rem;font-weight:750;letter-spacing:-.04em}.mark{display:grid;grid-template-columns:repeat(2,7px);gap:4px;transform:rotate(-15deg)}.mark i{display:block;width:7px;height:7px;border-radius:50%;background:var(--accent)}.mark i:nth-child(2),.mark i:nth-child(5){opacity:.35}.rail .sub{font-size:.78rem;color:var(--muted);margin-top:6px}.rail nav{display:flex;flex-direction:column;gap:5px}.rail nav a{color:var(--muted);font-size:.9rem;text-decoration:none;padding:9px 8px;border-radius:6px}.rail nav a:hover{background:var(--tint);color:var(--accent)}.rail nav a span{font-size:.75rem;display:inline-block;width:27px;color:#6c8187}.local{margin-top:auto;font-size:.8rem;color:var(--muted);border-top:1px solid var(--line);padding-top:18px}.local strong{display:block;color:var(--ink);margin-bottom:6px}.content{min-width:0;padding:30px 40px 48px}.topline{display:flex;align-items:center;justify-content:space-between;gap:12px;padding-bottom:24px;border-bottom:1px solid var(--line)}.eyebrow{font-size:.73rem;letter-spacing:.12em;text-transform:uppercase;color:var(--accent);font-weight:750}.pill{font-size:.75rem;border:1px solid #a3c3bd;border-radius:40px;padding:4px 10px;color:var(--accent);white-space:nowrap}.run-label{overflow-wrap:anywhere;font-size:.88rem;margin-top:5px;color:var(--muted)}.hero{padding:31px 0 25px}.hero .eyebrow{margin-bottom:12px}.hero p:not(.eyebrow){margin-top:14px;max-width:780px;color:var(--muted);font-size:1.05rem}.comparison{background:var(--paper);border:1px solid var(--line);border-radius:12px;padding:18px 20px;display:flex;align-items:end;gap:12px;flex-wrap:wrap}.field{display:flex;flex-direction:column;gap:6px;font-size:.8rem;font-weight:650}.comparison .field{flex:1;min-width:155px}.comparison select{width:100%}.comparison .rule-note{flex:1.6;min-width:210px;font-size:.8rem;color:var(--muted);padding:2px 0 3px 10px}.swap{font-size:1.2rem;min-height:43px}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:18px 0 28px}.metric{padding:17px 18px;border:1px solid var(--line);border-radius:10px;background:var(--paper)}.metric strong{display:block;font-variant-numeric:tabular-nums;font-size:1.9rem;font-weight:650;line-height:1.15;letter-spacing:-.03em}.metric span{font-size:.78rem;color:var(--muted);display:block;margin-top:7px}.section{margin-top:24px;padding:24px;background:var(--paper);border:1px solid var(--line);border-radius:12px;scroll-margin-top:20px}.section-head{display:flex;justify-content:space-between;align-items:start;gap:14px;margin-bottom:17px}.section-head p{margin-top:5px;color:var(--muted);font-size:.88rem}.legend{display:flex;flex-wrap:wrap;gap:14px;font-size:.77rem;color:var(--muted);margin:8px 0 19px}.legend span:before{content:"";display:inline-block;width:8px;height:8px;border-radius:2px;margin-right:6px;background:var(--color)}.unique{--color:var(--unique)}.ambiguous{--color:var(--ambiguous)}.none{--color:var(--none)}.invalid{--color:var(--invalid)}.outcome{display:grid;grid-template-columns:125px 1fr 82px;gap:14px;align-items:center;padding:9px 0;font-size:.83rem}.outcome svg{display:block;width:100%;height:20px;border-radius:4px}.outcome .percent{text-align:right;font-variant-numeric:tabular-nums}.fine{font-size:.78rem;color:var(--muted);margin-top:14px}.outcome-numbers{font-size:.78rem;color:var(--muted);font-variant-numeric:tabular-nums;margin:2px 0 10px 139px}.toolbar{display:flex;align-items:end;flex-wrap:wrap;gap:10px;margin:20px 0 10px}.toolbar .search{flex:1;min-width:180px}.toolbar input{width:100%}.scroll{overflow:auto;max-width:100%;border:1px solid var(--line);border-radius:8px}table{border-collapse:collapse;text-align:left;width:100%;font-size:.86rem;font-variant-numeric:tabular-nums}th,td{padding:12px 14px;border-bottom:1px solid var(--line);vertical-align:top}thead{background:#f6f8f7}thead th{color:var(--muted);font-size:.75rem;font-weight:650;white-space:nowrap}tr:last-child td,tr:last-child th{border-bottom:0}td.number,th.number{text-align:right;white-space:nowrap}.guide-scroll{max-height:560px}.guide-table{min-width:590px}.guide-table thead{position:sticky;top:0;z-index:1}.guide-table td:first-child{max-width:300px}.guide-table .target{padding:0;border:0;color:var(--accent);background:none;text-align:left;font-size:.86rem;overflow-wrap:anywhere;max-width:100%;font-weight:650}.gene{display:block;font-size:.73rem;color:var(--muted);margin-top:3px;overflow-wrap:anywhere}.delta{font-weight:700}.increase{color:#146779}.decrease{color:#86582a}.sort{border:0;background:none;padding:0;color:inherit;font:inherit;font-weight:650}.pagination{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:10px;margin-top:14px;font-size:.79rem;color:var(--muted)}.pages{display:flex;gap:8px}.empty{padding:30px!important;text-align:center;color:var(--muted)}.notice{padding:13px 16px;background:#f0f5f4;border-left:3px solid #8ab4ad;border-radius:0 6px 6px 0;font-size:.85rem;margin-top:18px}.matrix{min-width:540px;table-layout:fixed}.matrix th{font-size:.77rem}.matrix td{padding:5px;vertical-align:middle}.cell{width:100%;min-height:63px;border-color:transparent;border-radius:6px;text-align:center;background:#f4f7f6;padding:8px}.cell[data-level="1"]{background:#e4efec}.cell[data-level="2"]{background:#cbe2dd}.cell[data-level="3"]{background:#a4cec5}.cell[aria-pressed="true"]{border:2px solid var(--accent)}.cell strong{display:block;font-size:1.12rem;font-weight:650}.cell span{font-size:.71rem;color:var(--muted)}.read-panel{padding:15px 0 0}.read-panel p{font-size:.85rem;margin-top:6px;color:var(--muted)}.attach{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-top:17px;padding-top:16px;border-top:1px solid var(--line)}.attach .field{flex:1;min-width:200px}.attach input{font-size:.8rem;padding:6px;max-width:100%;width:100%;border-color:var(--line)}.attachment-status{flex-basis:100%;font-size:.8rem;color:var(--muted)}.attachment-status.error{color:#963c35}.read-table{min-width:590px;font-size:.8rem}.read-table td{max-width:230px;overflow-wrap:anywhere}.read-table small{display:block}.provenance-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px}.provenance-grid>div{min-width:0}.provenance-grid p{margin-top:6px;font-size:.84rem;color:var(--muted)}.identity{padding:14px 0;border-bottom:1px solid var(--line)}.identity:last-child{border-bottom:0}.identity small{display:block;margin:4px 0}.identity code{display:block;font-size:.73rem;color:var(--muted);user-select:all}.identity .filename{overflow-wrap:anywhere;font-weight:600}details{margin-top:18px;border-top:1px solid var(--line);padding-top:15px}summary{cursor:pointer;font-size:.9rem;font-weight:650}.policies{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px;margin-top:16px}.policies p{font-size:.82rem;color:var(--muted);margin-top:5px}.footer{font-size:.78rem;color:var(--muted);padding:24px 0 0;max-width:900px}.footer p+p{margin-top:7px}dialog{color:var(--ink);border:1px solid var(--line);border-radius:14px;width:min(800px,calc(100vw - 28px));max-height:calc(100vh - 40px);padding:0;box-shadow:0 24px 80px #152c3840}dialog::backdrop{background:#152c3855}.dialog-head{position:sticky;top:0;background:#fff;padding:22px 24px 18px;border-bottom:1px solid var(--line);z-index:1;display:flex;justify-content:space-between;align-items:start;gap:16px}.dialog-head h2{overflow-wrap:anywhere}.dialog-body{padding:20px 24px 26px}.detail-counts{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-bottom:18px}.detail-counts div{border:1px solid var(--line);border-radius:8px;padding:12px}.detail-counts strong{display:block;font-size:1.5rem}.detail-counts span{font-size:.78rem;color:var(--muted)}.dialog-body p{margin:12px 0;color:var(--muted);font-size:.9rem}.fallback{max-width:1100px;margin:auto;padding:30px}.fallback table{margin:20px 0}.fallback h2{margin-top:24px}.error-banner{padding:15px;margin:15px;background:#fff1ef;border:1px solid #b46b60}.sr-only{position:absolute!important;width:1px;height:1px;margin:-1px;padding:0;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}svg .unique{fill:var(--unique)}svg .ambiguous{fill:var(--ambiguous)}svg .none{fill:var(--none)}svg .invalid{fill:var(--invalid)}
@media(max-width:1100px){.rail{padding:27px 16px}.app{grid-template-columns:175px minmax(0,1fr)}.content{padding:25px}.metrics{gap:9px}.metric{padding:14px}.metric strong{font-size:1.7rem}.comparison .rule-note{flex-basis:100%;padding-left:0}}
@media(max-width:760px){.app{display:block}.rail{position:static;height:auto;padding:17px 20px;gap:10px;border-right:0;border-bottom:1px solid var(--line);flex-direction:row;align-items:center;justify-content:space-between}.rail nav,.rail .sub,.local{display:none}.content{padding:19px 16px 32px}.topline{padding-bottom:15px}.hero{padding:24px 0 20px}.metrics{grid-template-columns:repeat(2,minmax(0,1fr));margin-bottom:20px}.section{padding:18px 15px;margin-top:18px}.comparison{padding:15px;gap:8px}.comparison .field{min-width:110px}.comparison label{font-size:.74rem}.comparison select{font-size:.81rem;padding:8px 4px}.swap{padding:7px;font-size:1rem}.rule-note{font-size:.75rem!important}.section-head{flex-wrap:wrap}.outcome{grid-template-columns:85px 1fr 72px;gap:8px}.outcome-numbers{margin-left:0;font-size:.73rem}.provenance-grid{grid-template-columns:1fr;gap:6px}.policies{grid-template-columns:1fr}.dialog-head{padding:17px}.dialog-body{padding:17px}.detail-counts{gap:6px}.detail-counts div{padding:9px}.detail-counts span{font-size:.7rem}.toolbar .search{min-width:150px}.toolbar button{font-size:.8rem}.topline .pill{font-size:.68rem}.guide-table{min-width:0;table-layout:fixed}.guide-table th:first-child{width:36%}.guide-table th:not(:first-child){width:21.33%;white-space:normal}.guide-table td,.guide-table th{padding:10px 6px;font-size:.75rem}.guide-table th:last-child,.guide-table td:last-child{display:none}.guide-table .target{font-size:.77rem;padding:4px 0}.guide-table .gene{font-size:.67rem}.matrix{min-width:0}.matrix th{font-size:.66rem;padding:7px 3px;white-space:normal}.matrix td{padding:2px}.cell{padding:6px 2px;min-height:61px}.cell strong{font-size:.9rem;overflow-wrap:anywhere}.cell span{font-size:.62rem}.guide-scroll{max-height:460px}}
@media(prefers-reduced-motion:no-preference){button,a,input,select{transition:background-color .12s,border-color .12s}html{scroll-behavior:smooth}}
@media print{body{background:#fff;font-size:10pt}.app{display:block}.rail,.comparison,.toolbar,.pages,.attach,.topline .pill,.print-button,.skip{display:none!important}.content{padding:0}.section{break-inside:avoid;border:1px solid #aaa;border-radius:0}.scroll{overflow:visible}.hero{padding:16px 0}h1{font-size:24pt}.metric strong{font-size:20pt}.cell{border:1px solid #ccc}.footer{font-size:8pt}dialog,.read-table{display:none}.guide-scroll{max-height:none}.print-note{display:block!important}.guide-table{min-width:0}th,td{padding:7px}.guide-table .target{color:#000}.matrix{min-width:0}}
'''

SHELL = r'''
<a class="skip" href="#overview">Skip to review</a>
<div id="boot-error" class="error-banner" role="alert" hidden></div>
<div class="app" id="app" hidden>
 <aside class="rail" aria-label="Report navigation"><div><div class="brand"><span class="mark" aria-hidden="true"><i></i><i></i><i></i><i></i><i></i><i></i></span>DotMatch</div><p class="sub">EVIDENCE / ASSIGNMENT</p></div>
 <nav><a href="#overview"><span>01</span>Overview</a><a href="#guides"><span>02</span>Compare guides</a><a href="#transitions"><span>03</span>Read transitions</a><a href="#provenance"><span>04</span>Provenance</a></nav>
 <div class="local"><strong>Local. Read-only.</strong>No upload, external requests or saved browser state. This report does not run an analysis.</div></aside>
 <main class="content" id="overview" tabindex="-1"><header class="topline"><div><p class="eyebrow">Assignment review</p><p class="run-label" id="run-label"></p></div><span class="pill">Portable HTML · offline</span></header>
 <section class="hero" aria-labelledby="headline"><p class="eyebrow" id="comparison-label"></p><h1 id="headline"></h1><p id="hero-description"></p></section>
 <div class="comparison" aria-label="Compare recorded policies"><label class="field">Reference policy<select id="left" aria-label="Reference policy"></select></label><button class="swap" id="swap" type="button" aria-label="Swap reference and comparison policies">⇄</button><label class="field">Comparison policy<select id="right" aria-label="Comparison policy"></select></label><p class="rule-note">Switch between recorded results. No reads are reanalysed and no policy is recommended.</p></div>
 <div class="metrics"><div class="metric"><strong id="metric-reads"></strong><span>Total input reads</span></div><div class="metric"><strong id="metric-targets"></strong><span>Targets in the library</span></div><div class="metric"><strong id="metric-delta"></strong><span>Net change in unique assignments</span></div><div class="metric"><strong id="metric-guides"></strong><span>Guide counts that differ</span></div></div>
 <section class="section" aria-labelledby="outcomes-title"><div class="section-head"><div><h2 id="outcomes-title">Every read, accounted for</h2><p>All three policies use the same input reads and extraction window.</p></div><button type="button" id="export-figure">Export SVG figure</button></div><div class="legend" aria-label="Read outcomes"><span class="unique">Unique</span><span class="ambiguous">Ambiguous</span><span class="none">Unmatched</span><span class="invalid">Invalid window</span></div><div id="outcomes"></div><p class="fine" id="global-changes"></p></section>
 <section class="section" id="guides" aria-labelledby="guides-title"><div class="section-head"><div><h2 id="guides-title">Follow the count changes</h2><p>Select a guide to inspect its recorded counts and available read evidence.</p></div><button type="button" id="export">Export filtered TSV</button></div>
 <div class="toolbar"><label class="field search">Find a guide or gene<input type="search" id="search" placeholder="Search the full library" autocomplete="off" spellcheck="false"></label><label class="field">Show<select id="filter"><option value="changed">Changed counts</option><option value="all">All guides</option><option value="increased">Increased counts</option><option value="decreased">Decreased counts</option><option value="zero">Zero in both policies</option></select></label><button type="button" id="reset">Reset filters</button></div>
 <div class="scroll guide-scroll" id="guide-scroll" tabindex="0" role="region" aria-label="Guide-count comparison"><table class="guide-table"><caption class="sr-only" id="guide-caption"></caption><thead><tr><th scope="col" id="sort-id"><button class="sort" data-sort="id">Guide / gene</button></th><th scope="col" class="number" id="sort-left"><button class="sort" data-sort="left" id="left-heading"></button></th><th scope="col" class="number" id="sort-right"><button class="sort" data-sort="right" id="right-heading"></button></th><th scope="col" class="number" id="sort-delta"><button class="sort" data-sort="delta">Δ Count</button></th><th scope="col" class="number" id="sort-magnitude" aria-sort="descending"><button class="sort" data-sort="magnitude">|Δ|</button></th></tr></thead><tbody id="guide-rows"></tbody></table></div>
 <div class="pagination"><p id="page-label" role="status"></p><div class="pages"><button type="button" id="prev">Previous</button><button type="button" id="next">Next</button></div></div><p class="fine print-note" hidden>Printed tables contain the currently displayed page only. Export the filtered TSV to retain every matching guide.</p></section>
 <section class="section" id="transitions" aria-labelledby="transitions-title"><div class="section-head"><div><h2 id="transitions-title">Where did the reads move?</h2><p id="transition-description"></p></div></div><div class="scroll" tabindex="0" role="region" aria-label="Read-state transition matrix"><table class="matrix"><caption class="sr-only" id="matrix-caption"></caption><thead id="matrix-head"></thead><tbody id="matrix-body"></tbody></table></div><div class="read-panel"><h3 id="transition-selection">Select a cell to inspect a read-state transition.</h3><p id="transition-detail">The diagonal means the same state, not necessarily the same target. A transition matrix alone cannot establish identical read membership.</p><div id="transition-reads"></div></div>
 <div class="attach"><label class="field">Attach recorded read decisions (optional)<input type="file" id="read-file" accept=".tsv,text/tab-separated-values" aria-describedby="attachment-status"></label><button type="button" id="clear-reads" disabled>Clear attachment</button><p class="attachment-status" id="attachment-status" role="status"></p></div></section>
 <section class="section" id="provenance" aria-labelledby="provenance-title"><div class="section-head"><div><h2 id="provenance-title">What supports this review?</h2><p>Separate recorded evidence, internal consistency and scientific interpretation.</p></div><button type="button" class="print-button" id="print">Print review</button></div>
 <div class="provenance-grid"><div><h3>Reconciled artifact snapshot</h3><p id="snapshot-status"></p><p>Per-guide totals match unique outcomes. Transition margins account for all reads. These checks do not authenticate the report or validate the assay.</p><div id="artifact-identities"></div></div><div><h3>Recorded input identities</h3><p>Input files are not embedded or re-read. A recorded checksum is not proof of the biological sample, source authenticity or appropriateness.</p><div id="input-identities"></div></div></div>
 <div class="notice" id="window-note"></div><details><summary>Matching rules and interpretation limits</summary><div class="policies"><div><h3>Exact</h3><p>Exactly one target at distance zero. Duplicate target sequences remain ambiguous.</p></div><div><h3>Radius k=1</h3><p>Exactly one target anywhere within one substitution. Even an exact hit is ambiguous when another target is in that radius.</p></div><div><h3>Best k=1</h3><p>A unique nearest target within one substitution. Equally near targets remain ambiguous.</p></div></div><p class="fine">Quality characters are validated, not used to weight assignments. N and IUPAC symbols are literal, not wildcards. No indels, offset inference, reverse-complement search, cell/UMI processing or downstream screen statistics. Unique is an assignment outcome, not proof of biological origin.</p></details>
 <details><summary>Software and renderer identities</summary><div id="software-identities"></div></details></section>
 <footer class="footer"><p><strong>More assignments do not establish greater biological accuracy.</strong> Count-matrix identity and read-assignment identity are separate observations.</p><p>This portable report contains sample and target identifiers. Review them before sharing. Raw read sequences, quality strings and read IDs are not embedded. Optional attached read IDs remain in memory until cleared or the page is closed. They are excluded from this viewer’s print, count and figure exports; do not save the rendered page after attaching private records.</p></footer></main></div>
<dialog id="guide-dialog" aria-labelledby="detail-title"><div class="dialog-head"><div><p class="eyebrow">Guide evidence</p><h2 id="detail-title"></h2><p class="muted" id="detail-gene"></p></div><button type="button" id="close-dialog" aria-label="Close guide evidence">Close</button></div><div class="dialog-body"><div class="detail-counts" id="detail-counts"></div><h3 id="detail-summary"></h3><p id="detail-explanation"></p><h3>Recorded read decisions</h3><div id="detail-reads"></div><div class="notice">This bundle does not contain read sequences, qualities or enumerated candidate targets. A base-level alignment or a specific mismatch explanation cannot be inferred from these counts.</div></div></dialog>
<div class="sr-only" id="announcement" role="status" aria-live="polite"></div>
'''

JS = r'''
"use strict";
(() => {
const MODES = ["exact", "radius_k1", "best_k1"], STATES = ["unique", "ambiguous", "none", "invalid"];
const LABEL = {exact:"Exact", radius_k1:"Radius k=1", best_k1:"Best k=1", unique:"Unique", ambiguous:"Ambiguous", none:"Unmatched", invalid:"Invalid window"};
const $ = id => document.getElementById(id), fmt = n => n.toLocaleString("en-US"), signed = n => (n > 0 ? "+" : n < 0 ? "−" : "") + fmt(Math.abs(n));
const el = (tag, text, cls) => {const node = document.createElement(tag); if(text !== undefined) node.textContent = text; if(cls) node.className = cls; return node;};
const append = (id, node) => $(id).append(node);
const percent = (n,total) => !total ? "0.0%" : n>0 && 100*n/total<.001 ? "<0.001%" : (100*n/total).toFixed(n>0 && 100*n/total<.1 ? 3 : 1)+"%";
let d, s, left = "exact", right = "radius_k1", page = 0, sort = "magnitude", direction = -1, visible = [], selected = null, attached = null, openedGuide = null, origin = null, loadToken = 0, searchTimer;
const SIZE = 50, MAX_READ_BYTES = 32*1024*1024, MAX_READ_ROWS = 100000;
const modeIndex = m => MODES.indexOf(m) + 2;
const changedForPair = r => r[left+"_status"] !== r[right+"_status"] || r[left+"_target_id"] !== r[right+"_target_id"];
const transitionCount = (a,b) => {let row=d.transitions.find(t=>t[0]===left && t[1]===right && t[2]===a && t[3]===b); if(row)return row[4]; row=d.transitions.find(t=>t[0]===right && t[1]===left && t[2]===b && t[3]===a); if(!row)throw Error("Missing transition");return row[4];};
function validateEmbedded(data){
 if(!data || data.schema_version!=="dotmatch.review.v1" || data.summary?.schema_version!=="dotmatch.sensitivity.v1" || data.summary.completion!=="complete")throw Error("Unsupported or incomplete review data.");
 const sum=data.summary, integer=n=>Number.isSafeInteger(n)&&n>=0;
 if(!integer(sum.read_count)||sum.read_count===0||!integer(sum.target_count)||sum.target_count===0||sum.target_count>250000||!integer(sum.changed_reads)||sum.changed_reads>sum.read_count||!Array.isArray(data.guides)||data.guides.length!==sum.target_count||!Array.isArray(data.transitions)||data.transitions.length!==48)throw Error("Invalid review dimensions.");
 const seen=new Set(), totals=[0,0,0];
 for(const g of data.guides){if(!Array.isArray(g)||g.length!==5||typeof g[0]!=="string"||!g[0]||typeof g[1]!=="string"||seen.has(g[0])||g.slice(2).some(n=>!integer(n)))throw Error("Invalid guide data.");seen.add(g[0]);for(let i=0;i<3;i++)totals[i]+=g[i+2];}
 for(let i=0;i<3;i++){const o=sum.outcomes?.[MODES[i]];if(!o||STATES.some(k=>!integer(o[k]))||STATES.reduce((n,k)=>n+o[k],0)!==sum.read_count||totals[i]!==o.unique)throw Error("Read/count conservation failed.");}
 const keys=new Set();
 for(const t of data.transitions){if(!Array.isArray(t)||t.length!==5||MODES.indexOf(t[0])<0||MODES.indexOf(t[1])<=MODES.indexOf(t[0])||!STATES.includes(t[2])||!STATES.includes(t[3])||!integer(t[4])||keys.has(t.slice(0,4).join("|")))throw Error("Invalid transition data.");keys.add(t.slice(0,4).join("|"));}
 for(let i=0;i<3;i++)for(let j=i+1;j<3;j++){const rows=data.transitions.filter(t=>t[0]===MODES[i]&&t[1]===MODES[j]);for(const state of STATES)if(rows.filter(t=>t[2]===state).reduce((n,t)=>n+t[4],0)!==sum.outcomes[MODES[i]][state]||rows.filter(t=>t[3]===state).reduce((n,t)=>n+t[4],0)!==sum.outcomes[MODES[j]][state])throw Error("Transition conservation failed.");}
}
function options(){for(const id of ["left","right"]){for(const m of MODES){const o=el("option",LABEL[m]);o.value=m;$(id).append(o);}}}
function outcomes(){
 $("outcomes").replaceChildren();
 for(const mode of MODES){const box=el("div"), row=el("div",undefined,"outcome"); row.append(el("strong",LABEL[mode]));const ns="http://www.w3.org/2000/svg", svg=document.createElementNS(ns,"svg");svg.setAttribute("viewBox","0 0 1000 20");svg.setAttribute("preserveAspectRatio","none");svg.setAttribute("role","img");svg.setAttribute("aria-label",LABEL[mode]+": "+STATES.map(k=>fmt(s.outcomes[mode][k])+" "+LABEL[k].toLowerCase()).join(", "));let offset=0;for(const state of STATES){const width=1000*s.outcomes[mode][state]/s.read_count, rect=document.createElementNS(ns,"rect");rect.setAttribute("x",String(offset));rect.setAttribute("width",String(width));rect.setAttribute("height","20");rect.setAttribute("class",state);svg.append(rect);offset+=width;}row.append(svg,el("span",percent(s.outcomes[mode].unique,s.read_count)+" unique","percent"));box.append(row,el("p",STATES.map(k=>fmt(s.outcomes[mode][k])+" "+LABEL[k].toLowerCase()).join(" · "),"outcome-numbers"));$("outcomes").append(box);}
 $("global-changes").textContent=fmt(s.changed_reads)+" reads change state or assigned target across ANY of the three policies. This is not the selected pair’s changed-read count.";
}
function summary(){
 const a=modeIndex(left),b=modeIndex(right), differing=d.guides.reduce((n,g)=>n+(g[a]!==g[b]),0), delta=s.outcomes[right].unique-s.outcomes[left].unique;
 $("left").value=left;$("right").value=right;
 for(const o of $("left").options)o.disabled=o.value===right;
 for(const o of $("right").options)o.disabled=o.value===left;
 $("comparison-label").textContent=LABEL[left]+" → "+LABEL[right];
 $("headline").textContent=delta===0&&differing>0?"Same assigned total. Different guide counts.":differing===0?"No guide-count differences in this comparison.":fmt(differing)+" guide counts change with the rule.";
 $("hero-description").textContent=differing===0?"The two count matrices agree. That alone does not establish identical read membership or biological accuracy.":fmt(differing)+" of "+fmt(s.target_count)+" targets have different counts. "+(delta===0?"The assigned-read percentage would hide this difference.":"Unique assignments change by "+signed(delta)+" reads. More assignments are not necessarily better assignments.");
 $("metric-reads").textContent=fmt(s.read_count);$("metric-targets").textContent=fmt(s.target_count);$("metric-delta").textContent=signed(delta);$("metric-guides").textContent=fmt(differing);
 $("left-heading").textContent=LABEL[left];$("right-heading").textContent=LABEL[right];$("guide-caption").textContent="Guide counts: "+LABEL[left]+" versus "+LABEL[right];
 $("transition-description").textContent="Rows: "+LABEL[left]+". Columns: "+LABEL[right]+". Percentages use all "+fmt(s.read_count)+" input reads.";
 $("matrix-caption").textContent=$("transition-description").textContent;
}
function sortValue(g){const a=g[modeIndex(left)],b=g[modeIndex(right)];return sort==="id"?g[0]:sort==="left"?a:sort==="right"?b:sort==="delta"?b-a:Math.abs(b-a);}
function filterGuides(){const q=$("search").value.trim().toLocaleLowerCase(), f=$("filter").value,a=modeIndex(left),b=modeIndex(right);visible=d.guides.filter(g=>(!q||(g[0]+"\n"+g[1]).toLocaleLowerCase().includes(q))&&(f==="all"||f==="changed"&&g[a]!==g[b]||f==="increased"&&g[b]>g[a]||f==="decreased"&&g[b]<g[a]||f==="zero"&&g[a]===0&&g[b]===0));visible.sort((a,b)=>{const x=sortValue(a),y=sortValue(b);return direction*(typeof x==="string"?x.localeCompare(y):x-y)||a[0].localeCompare(b[0]);});page=Math.min(page,Math.max(0,Math.ceil(visible.length/SIZE)-1));renderGuides();}
function renderGuides(){
 const tbody=$("guide-rows"),frag=document.createDocumentFragment(),a=modeIndex(left),b=modeIndex(right);tbody.replaceChildren();
 for(const g of visible.slice(page*SIZE,(page+1)*SIZE)){const tr=el("tr"),cell=el("td"),button=el("button",g[0],"target");button.type="button";button.addEventListener("click",()=>openGuide(g,button));cell.append(button,el("span",g[1]||"No gene label","gene"));tr.append(cell,el("td",fmt(g[a]),"number"),el("td",fmt(g[b]),"number"),el("td",signed(g[b]-g[a]),"number delta "+(g[b]>g[a]?"increase":g[b]<g[a]?"decrease":"")),el("td",fmt(Math.abs(g[b]-g[a])),"number"));frag.append(tr);}
 if(!visible.length){const tr=el("tr"),td=el("td","No guides match these filters. Choose All guides or clear the search.","empty");td.colSpan=5;tr.append(td);frag.append(tr);}
 tbody.append(frag);$("page-label").textContent=visible.length?fmt(page*SIZE+1)+"–"+fmt(Math.min((page+1)*SIZE,visible.length))+" of "+fmt(visible.length)+" matching guides · "+fmt(d.guides.length)+" in library":"0 matching guides · "+fmt(d.guides.length)+" in library";
 $("prev").disabled=page===0;$("next").disabled=(page+1)*SIZE>=visible.length;$("export").disabled=visible.length===0;
 for(const k of ["id","left","right","delta","magnitude"]){const th=$("sort-"+k);if(k===sort)th.setAttribute("aria-sort",direction===1?"ascending":"descending");else th.removeAttribute("aria-sort");}
}
function matrix(){
 const head=el("tr");head.append(el("th","From ↓ / To →"));for(const state of STATES){const th=el("th",LABEL[state]);th.scope="col";head.append(th);}$("matrix-head").replaceChildren(head);$("matrix-body").replaceChildren();
 for(const a of STATES){const row=el("tr"),heading=el("th",LABEL[a]);heading.scope="row";row.append(heading);for(const b of STATES){const n=transitionCount(a,b),td=el("td"),button=el("button",undefined,"cell");button.type="button";button.dataset.level=String(n===0?0:n/s.read_count<.1?1:n/s.read_count<.3?2:3);button.setAttribute("aria-label",LABEL[a]+" to "+LABEL[b]+": "+fmt(n)+" reads");button.setAttribute("aria-pressed",String(selected?.[0]===a&&selected?.[1]===b));button.append(el("strong",fmt(n)),el("span",percent(n,s.read_count)));button.dataset.from=a;button.dataset.to=b;button.addEventListener("click",()=>{selected=[a,b];matrix();transitionDetail();document.querySelector('[data-from="'+a+'"][data-to="'+b+'"]').focus({preventScroll:true});});td.append(button);row.append(td);}$("matrix-body").append(row);}
}
function decisionReason(r){
 if(r.exact_status==="unique" && r.radius_k1_status==="ambiguous")return "An exact hit is recorded, but radius-one also admits another target within one substitution. Radius abstains; best-distance retains the unique nearest target. The candidate sequences are not included.";
 if(r.exact_status==="none" && r.best_k1_status==="unique")return "No exact target is recorded. Both mismatch-tolerant policies admit the same single target within one substitution. This does not establish the read’s biological origin.";
 if(r.exact_status==="none" && r.best_k1_status==="ambiguous")return "No exact target is recorded. Multiple targets tie within one substitution, so both mismatch-tolerant rules abstain. The count tables do not identify those candidates.";
 return "These are the recorded policy decisions. No sequence-level explanation can be reconstructed from this artifact.";
}
function validPolicyCalls(r){
 const e=r.exact_status,a=r.radius_k1_status,b=r.best_k1_status;
 if(e==="invalid")return a==="invalid"&&b==="invalid";
 if(e==="ambiguous")return a==="ambiguous"&&b==="ambiguous";
 if(e==="unique")return b==="unique"&&r.exact_target_id===r.best_k1_target_id&&(a==="ambiguous"||a==="unique"&&r.radius_k1_target_id===r.exact_target_id);
 return e==="none"&&a===b&&a!=="invalid"&&r.radius_k1_target_id===r.best_k1_target_id;
}
function readTable(rows, destination){
 destination.replaceChildren();if(!attached){destination.append(el("p","Attach the original read_changes.tsv below to inspect recorded decisions. It is checked against this report’s recorded hash and aggregate counts; this does not authenticate the report."));return;}
 if(!rows.length){destination.append(el("p","No changed read records match this selection. Unchanged records are not present in read_changes.tsv."));return;}
 destination.append(el("p","Showing "+fmt(Math.min(rows.length,100))+" of "+fmt(rows.length)+" matching changed records. Record ordinals are 1-based; read IDs may repeat."));const wrap=el("div",undefined,"scroll"),table=el("table",undefined,"read-table"),head=el("thead"),hr=el("tr");for(const text of ["Record / read ID",LABEL[left],LABEL[right]]){const th=el("th",text);th.scope="col";hr.append(th);}head.append(hr);table.append(head);const body=el("tbody");for(const r of rows.slice(0,100)){const tr=el("tr"),id=el("td",fmt(r.record_index));id.append(el("small",r.read_id));const why=el("details"),whyTitle=el("summary","Why these calls?");why.append(whyTitle,el("p",decisionReason(r)));id.append(why);tr.append(id);for(const m of [left,right]){const td=el("td",LABEL[r[m+"_status"]]);if(r[m+"_target_id"])td.append(el("small",r[m+"_target_id"]));tr.append(td);}body.append(tr);}table.append(body);wrap.append(table);destination.append(wrap);
}
function transitionDetail(){
 const dest=$("transition-reads");if(!selected){dest.replaceChildren();return;}const[a,b]=selected,n=transitionCount(a,b);$("transition-selection").textContent=fmt(n)+" reads: "+LABEL[a]+" → "+LABEL[b];$("transition-detail").textContent=(a===b?"Same state does not necessarily mean the same assigned target. ":"These reads change assignment state between the selected policies. ")+"The optional file contains only records changed across any policy; it is not the complete population of this cell.";
 readTable(attached?attached.filter(r=>r[left+"_status"]===a&&r[right+"_status"]===b):[],dest);
}
function openGuide(g, trigger){openedGuide=g;origin=trigger||origin;$("detail-title").textContent=g[0];$("detail-gene").textContent=g[1]||"No gene label";$("detail-counts").replaceChildren();for(let i=0;i<3;i++){const cell=el("div");cell.append(el("strong",fmt(g[i+2])),el("span",LABEL[MODES[i]]));$("detail-counts").append(cell);}const a=g[modeIndex(left)],b=g[modeIndex(right)],delta=b-a;$("detail-summary").textContent=LABEL[left]+" → "+LABEL[right]+": "+signed(delta)+" counted reads";$("detail-explanation").textContent=delta===0?"This guide has the same count under both policies. A count alone cannot show whether the same reads contributed.":a===0?"No uniquely assigned reads under the reference policy; "+fmt(b)+" under the comparison policy. A percentage increase from zero is not defined.":"The count changes by "+(Math.abs(delta)/a*100).toFixed(1)+"% relative to "+LABEL[left]+" ("+(delta>0?"increase":"decrease")+"). This is sensitivity to the matching rule, not an estimate of biological accuracy.";
 readTable(attached?attached.filter(r=>changedForPair(r)&&(r[left+"_target_id"]===g[0]||r[right+"_target_id"]===g[0])):[],$("detail-reads"));if(!$("guide-dialog").open)$("guide-dialog").showModal();}
function identity(dest,name,info){const box=el("div",undefined,"identity");box.append(el("span",name,"filename"));if(info.bytes!==undefined)box.append(el("small",fmt(info.bytes)+" bytes · recorded SHA-256"));else box.append(el("small","Recorded SHA-256 · input bytes not verified here"));box.append(el("code",info.sha256));$(dest).append(box);}
function provenance(){
 $("run-label").textContent=s.sample_label+" · DotMatch "+s.software_version;
 $("snapshot-status").textContent=d.source_manifest_checked?"The renderer checked guide and transition artifact hashes against the supplied completion manifest. The manifest itself was not authenticated.":"Generated from the producer’s staged artifacts. Confirm completed publication using summary.json beside the original report. The HTML alone does not recheck those files.";
 for(const name of ["guide_deltas.tsv","transitions.tsv"])identity("artifact-identities",name,d.sources[name]);
 for(const kind of ["targets","reads"])identity("input-identities",s.inputs[kind].name,s.inputs[kind]);
 for(const [label,hash] of [["Analysis implementation",s.implementation_sha256],["Native library",s.native_library_sha256],["Review renderer",d.renderer_sha256]])identity("software-identities",label,{sha256:hash});
 const p=s.parameters;$("window-note").textContent="Fixed extraction window ["+fmt(p.target_start)+", "+fmt(p.target_start+p.target_length)+") · zero-based, end-exclusive · "+fmt(p.target_length)+" bases · supplied orientation · Hamming substitutions only.";
 $("attachment-status").textContent=d.sources["read_changes.tsv"]?"Nothing attached. Choose the original read_changes.tsv; maximum 32 MiB / 100,000 changed records. Read IDs are not embedded in this report.":"This run did not record read changes. A future run needs --write-read-changes. Counts cannot reconstruct absent read evidence.";
 $("read-file").disabled=!d.sources["read_changes.tsv"];
}
function parseTSV(text){
 // Strict RFC-style quoted fields, including embedded tabs/newlines and CRLF.
 const rows=[];let row=[],value="",quoted=false,closed=false,atStart=true;
 if(text.charCodeAt(0)===0xfeff)text=text.slice(1);
 const field=()=>{row.push(value);value="";closed=false;atStart=true;},record=()=>{field();rows.push(row);row=[];if(rows.length>MAX_READ_ROWS+1)throw Error("Read attachment exceeds 100,000 records; use the original TSV outside this viewer.");};
 for(let i=0;i<text.length;i++){const c=text[i];if(quoted){if(c==='"'){if(text[i+1]==='"'){value+='"';i++;}else{quoted=false;closed=true;}}else value+=c;}else if(c==='"'&&atStart){quoted=true;atStart=false;}else if(c==='\t'){field();}else if(c==='\n'||c==='\r'){if(c==='\r'&&text[i+1]==='\n')i++;record();}else{if(closed||c==='"')throw Error("Malformed quoted TSV field.");value+=c;atStart=false;}if(value.length>16384)throw Error("Read attachment contains an overlong field.");}
 if(quoted)throw Error("Unterminated quoted TSV field.");if(value!==""||closed||row.length)record();return rows;
}
function validateReadChanges(text){
 const columns=["record_index","read_id",...MODES.flatMap(m=>[m+"_status",m+"_target_id"])],rows=parseTSV(text),header=rows.shift();if(!header||JSON.stringify(header)!==JSON.stringify(columns))throw Error("Unsupported read_changes.tsv columns.");if(rows.length!==s.changed_reads)throw Error("Changed record count does not match this report.");
 const guideIds=new Set(d.guides.map(g=>g[0])),records=[],diffs=new Map(),off=new Map();let previous=0;
 for(const row of rows){if(row.length!==columns.length||!/^\d+$/.test(row[0]))throw Error("Malformed read-change record.");const r=Object.fromEntries(columns.map((c,i)=>[c,row[i]]));r.record_index=Number(r.record_index);if(!Number.isSafeInteger(r.record_index)||r.record_index<=previous||r.record_index>s.read_count||!r.read_id)throw Error("Invalid or duplicate record ordinal.");previous=r.record_index;
 for(const m of MODES){const state=r[m+"_status"],id=r[m+"_target_id"];if(!STATES.includes(state)||(state==="unique"?!guideIds.has(id):id!==""))throw Error("Unknown state or inconsistent unique target.");}
 if(!validPolicyCalls(r))throw Error("Recorded calls contradict fixed-window Hamming policy semantics.");
 if(MODES.every(m=>r[m+"_status"]===r.exact_status&&r[m+"_target_id"]===r.exact_target_id))throw Error("Unchanged record in changed-read file.");
 for(let i=0;i<3;i++)for(let j=i+1;j<3;j++){const a=MODES[i],b=MODES[j],as=r[a+"_status"],bs=r[b+"_status"];if(as!==bs){const k=[a,b,as,bs].join("|");off.set(k,(off.get(k)||0)+1);}for(const[m,sign]of[[a,-1],[b,1]])if(r[m+"_status"]==="unique"){const k=JSON.stringify([a,b,r[m+"_target_id"]]);diffs.set(k,(diffs.get(k)||0)+sign);}}
 records.push(r);}
 for(const t of d.transitions)if(t[2]!==t[3]&&(off.get(t.slice(0,4).join("|"))||0)!==t[4])throw Error("Read changes contradict state transitions.");
 for(let i=0;i<3;i++)for(let j=i+1;j<3;j++)for(const g of d.guides)if((diffs.get(JSON.stringify([MODES[i],MODES[j],g[0]]))||0)!==g[j+2]-g[i+2])throw Error("Read changes contradict guide-count deltas.");
 return records;
}
async function attach(){
 const file=$("read-file").files[0],token=++loadToken;if(!file){clearReads();return;}attached=null;$("clear-reads").disabled=false;$("attachment-status").classList.remove("error");$("attachment-status").textContent="Checking the attachment locally…";refreshReadPanels();
 try{const expected=d.sources["read_changes.tsv"];if(!expected)throw Error("This report has no recorded read-change artifact.");if(file.size>MAX_READ_BYTES)throw Error("File exceeds the 32 MiB viewer limit. No records were loaded; use the original TSV outside the viewer.");if(file.size!==expected.bytes)throw Error("File size does not match the recorded artifact.");if(!globalThis.crypto?.subtle)throw Error("This browser context cannot check SHA-256. No read records were loaded; open this report in a browser context supporting Web Crypto.");const bytes=await file.arrayBuffer(),digest=await crypto.subtle.digest("SHA-256",bytes);if(token!==loadToken)return;const hex=Array.from(new Uint8Array(digest),n=>n.toString(16).padStart(2,"0")).join("");if(hex!==expected.sha256)throw Error("SHA-256 mismatch. This is not the read-change artifact recorded in this report.");const text=new TextDecoder("utf-8",{fatal:true}).decode(bytes),records=validateReadChanges(text);if(token!==loadToken)return;attached=records;$("attachment-status").textContent=fmt(records.length)+" changed records loaded. Hash matches this report; transitions and guide deltas reconcile. This is consistency, not authentication. Clear to remove read IDs.";refreshReadPanels();}
 catch(error){if(token!==loadToken)return;attached=null;$("attachment-status").classList.add("error");$("attachment-status").textContent=String(error.message||error);refreshReadPanels();}
}
function refreshReadPanels(){transitionDetail();if(openedGuide&&$("guide-dialog").open)openGuide(openedGuide);else $("detail-reads").replaceChildren();}
function clearReads(){loadToken++;attached=null;$("read-file").value="";$("clear-reads").disabled=true;$("attachment-status").classList.remove("error");$("attachment-status").textContent="Attachment cleared. No read IDs are retained by the viewer.";refreshReadPanels();}
function exportFigure(){
 const ns="http://www.w3.org/2000/svg",svg=document.createElementNS(ns,"svg"),rows=visible.slice(0,12),height=415+rows.length*55;
 svg.setAttribute("xmlns",ns);svg.setAttribute("viewBox","0 0 1100 "+height);svg.setAttribute("width","1100");svg.setAttribute("height",String(height));svg.setAttribute("role","img");
 const add=(name,attrs,text)=>{const n=document.createElementNS(ns,name);for(const[k,v]of Object.entries(attrs||{}))n.setAttribute(k,String(v));if(text!==undefined)n.textContent=text;svg.append(n);return n;};
 const txt=(x,y,text,size=15,fill="#192d38",weight=400)=>add("text",{x,y,fill,"font-size":size,"font-family":"system-ui, sans-serif","font-weight":weight},text);
 const shorten=(text,n=72)=>Array.from(text).length>n?Array.from(text).slice(0,n-1).join("")+"…":text;
 add("title",{},"DotMatch assignment sensitivity: "+LABEL[left]+" versus "+LABEL[right]);
 add("desc",{},"Recorded read outcomes and the first "+rows.length+" of "+visible.length+" filtered guides. Matching-policy sensitivity, not biological accuracy. No read IDs are exported.");
 add("metadata",{},JSON.stringify({schema_version:"dotmatch.review.figure.v1",left,right,filter:$("filter").value,search:$("search").value,shown:rows.length,matching:visible.length,sources:d.sources,renderer_sha256:d.renderer_sha256}));
 add("rect",{width:1100,height,fill:"#ffffff"});txt(45,45,"DOTMATCH / ASSIGNMENT REVIEW",13,"#165e60",700);txt(45,83,LABEL[left]+" → "+LABEL[right],28,"#192d38",650);txt(45,111,shorten(s.sample_label),14,"#576b75");
 txt(45,145,fmt(s.read_count)+" input reads · "+fmt(s.target_count)+" library targets · raw unique-assignment counts",14,"#576b75");
 const colors=["#27777a","#a16c19","#7c8c98","#846ca4"];
 [left,right].forEach((mode,i)=>{const y=176+i*49;txt(45,y+15,LABEL[mode],14,"#192d38",650);let x=195;STATES.forEach((state,j)=>{const width=690*s.outcomes[mode][state]/s.read_count;add("rect",{x,y,width,height:21,fill:colors[j]});x+=width;});txt(905,y+15,fmt(s.outcomes[mode].unique)+" unique",14);txt(195,y+37,STATES.map(k=>LABEL[k]+": "+fmt(s.outcomes[mode][k])).join(" · "),11,"#576b75");});
 txt(45,299,"Guide slice: "+rows.length+" of "+fmt(visible.length)+" matching guides (current filtered order)",16,"#192d38",650);txt(45,322,"Filter: "+$("filter").selectedOptions[0].textContent+" · Search: "+shorten($("search").value||"none",65),12,"#576b75");
 const a=modeIndex(left),b=modeIndex(right),max=Math.max(1,...rows.flatMap(g=>[g[a],g[b]]));
 rows.forEach((g,i)=>{const y=348+i*55;txt(45,y+18,shorten(g[0],25),13,"#192d38",600);add("rect",{x:300,y,width:500*g[a]/max,height:14,fill:"#7c8c98"});add("rect",{x:300,y:y+18,width:500*g[b]/max,height:14,fill:"#27777a"});txt(825,y+12,LABEL[left]+": "+fmt(g[a]),12);txt(825,y+31,LABEL[right]+": "+fmt(g[b])+" (Δ "+signed(g[b]-g[a])+")",12);});
 if(!rows.length)txt(45,355,"No guides match the current filters.",14,"#576b75");
 txt(45,height-35,"All guide bars share a linear 0–"+fmt(max)+" count scale. Full filtered counts are available via TSV export.",11,"#576b75");txt(45,height-15,"More assignments do not establish greater biological accuracy. Figure contains sample and target identifiers.",11,"#576b75");
 const url=URL.createObjectURL(new Blob([new XMLSerializer().serializeToString(svg)],{type:"image/svg+xml;charset=utf-8"})),link=el("a");link.href=url;link.download="dotmatch-"+left+"-vs-"+right+".svg";document.body.append(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);$("announcement").textContent="Exported SVG with "+rows.length+" of "+visible.length+" filtered guides. No read IDs included.";
}
function exportTSV(){
 // Protect text identifiers from spreadsheet formula interpretation; counts
 // remain exact integers. Quotes retain tabs/newlines within identifiers.
 const quote=(x,text=false)=>{let v=String(x);if(text&&/^[\s]*[=+@-]/.test(v))v="'"+v;return /[\t\r\n"]/.test(v)?'"'+v.replace(/"/g,'""')+'"':v;};
 const header=["target_id","gene",left,right,"comparison_minus_reference"],lines=[header.join("\t")];for(const g of visible)lines.push([quote(g[0],true),quote(g[1],true),g[modeIndex(left)],g[modeIndex(right)],g[modeIndex(right)]-g[modeIndex(left)]].join("\t"));const url=URL.createObjectURL(new Blob([lines.join("\n")+"\n"],{type:"text/tab-separated-values;charset=utf-8"})),a=el("a");a.href=url;a.download="dotmatch-"+left+"-vs-"+right+"-filtered.tsv";document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);$("announcement").textContent="Exported "+fmt(visible.length)+" matching guides. Formula-like text IDs are spreadsheet-escaped.";
}
function changePair(){clearTimeout(searchTimer);page=0;selected=null;$("transition-selection").textContent="Select a cell to inspect a read-state transition.";$("transition-detail").textContent="The diagonal means the same state, not necessarily the same target. A transition matrix alone cannot establish identical read membership.";summary();filterGuides();matrix();transitionDetail();$("announcement").textContent="Comparing "+LABEL[left]+" with "+LABEL[right]+".";}
try{
 d=JSON.parse($("review-data").textContent);validateEmbedded(d);s=d.summary;options();outcomes();provenance();changePair();
 $("left").addEventListener("change",()=>{left=$("left").value;changePair();});$("right").addEventListener("change",()=>{right=$("right").value;changePair();});$("swap").addEventListener("click",()=>{[left,right]=[right,left];changePair();});
 $("search").addEventListener("input",()=>{clearTimeout(searchTimer);searchTimer=setTimeout(()=>{page=0;filterGuides();},100);});$("filter").addEventListener("change",()=>{page=0;filterGuides();});$("reset").addEventListener("click",()=>{$("search").value="";$("filter").value="all";page=0;sort="magnitude";direction=-1;filterGuides();});
 for(const button of document.querySelectorAll("[data-sort]"))button.addEventListener("click",()=>{const next=button.dataset.sort;if(sort===next)direction*=-1;else{sort=next;direction=sort==="id"?1:-1;}page=0;filterGuides();});
 $("prev").addEventListener("click",()=>{page--;renderGuides();$("guide-scroll").scrollTop=0;});$("next").addEventListener("click",()=>{page++;renderGuides();$("guide-scroll").scrollTop=0;});$("export").addEventListener("click",exportTSV);$("export-figure").addEventListener("click",exportFigure);$("read-file").addEventListener("change",attach);$("clear-reads").addEventListener("click",clearReads);$("print").addEventListener("click",()=>window.print());$("close-dialog").addEventListener("click",()=>$("guide-dialog").close());$("guide-dialog").addEventListener("close",()=>{openedGuide=null;if(origin?.isConnected)origin.focus();});
 $("app").hidden=false;$("fallback").hidden=true;
}catch(error){$("app").hidden=true;$("fallback").hidden=false;$("boot-error").hidden=false;$("boot-error").textContent="Interactive review could not start: "+String(error.message||error)+" The static snapshot below is not a verification of the supplied data; inspect the original artifacts.";}
})();
'''
