# EditWitness 0.2.0a1

Research-alpha downloads built from `bbf186f7a6f8c441ee4c0ed54f71bfe7e78e5cef`.

[Verification run](https://github.com/dnncha/dotmatch/actions/runs/34001337182). Read `publication.json` for the actual publication status.

Install the wheel with `python -m pip install ./editwitness-0.2.0a1-py3-none-any.whl`. Verify its SHA-256 against `SHA256SUMS`. The source ZIP has independent package sources, not DotMatch history or temporary transport code.

The report and JSON example are explicitly synthetic. No experimental validation, clinical suitability, standalone repository or PyPI publication is implied.
