# Expected Output

The small fixture is deterministic. Running `../run.sh` should produce:

- `counts.tsv`
- `counts.mageck.tsv`
- `summary.json`
- `assignments.tsv`
- `ambiguous.tsv`
- `unmatched.tsv`

The exact full-data counts are not checked into the repo because the public FASTQ files are large.
